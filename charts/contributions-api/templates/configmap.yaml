apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "contributions-api.fullname" . }}-config
  labels:
    {{- include "contributions-api.labels" . | nindent 4 }}
data:
{{- range $envKey, $envVal := .Values.env }}
  {{ $envKey | upper }}: {{ $envVal | quote }}
{{- end }}
