apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "drone-runner-kube.fullname" . }}-config
  labels:
    {{- include "drone-runner-kube.labels" . | nindent 4 }}
data:
{{- range $envKey, $envVal := .Values.env }}
  {{ $envKey | upper }}: {{ $envVal | quote }}
{{- end }}
