apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drone-runner-kube.fullname" . }}-test-connection"
  labels:
    {{- include "drone-runner-kube.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drone-runner-kube.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
