apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailman.core.fullname" . }}-test-connection"
  labels:
    {{- include "mailman.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailman.core.fullname" . }}:{{ .Values.core.service.ports.api }}']
  restartPolicy: Never
