apiVersion: v1
kind: Service
metadata:
  name: {{ include "link.fullname" . }}
  labels:
    {{- include "link.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "link.selectorLabels" . | nindent 4 }}
