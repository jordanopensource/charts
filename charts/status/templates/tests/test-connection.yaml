apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "status.fullname" . }}-test-connection"
  labels:
    {{- include "status.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "status.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
