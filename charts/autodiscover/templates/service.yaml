apiVersion: v1
kind: Service
metadata:
  name: {{ include "josa-autodiscover.fullname" . }}
  labels:
    {{- include "josa-autodiscover.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "josa-autodiscover.selectorLabels" . | nindent 4 }}
