apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "josa-autodiscover.fullname" . }}-test-connection"
  labels:
    {{- include "josa-autodiscover.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "josa-autodiscover.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
