{{- if not .Values.indexer.config.indexerCustomConfig }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "wazuh.fullname" . }}-indexer-config
data:
{{ (.Files.Glob "config/indexer/opensearch.yml").AsConfig | indent 2 }}
{{- end }}
