apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rally.fullname" . }}-test-connection"
  labels:
    {{- include "rally.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rally.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
