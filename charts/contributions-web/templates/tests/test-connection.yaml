apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "contributions-web.fullname" . }}-test-connection"
  labels:
    {{- include "contributions-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "contributions-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
